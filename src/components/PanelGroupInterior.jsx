import '../App.css'

function PanelGroupInterior({isCollapsed}) {
    return (<div>
            {!isCollapsed && (
                "123"
            )}
        </div>
    );
}

export default PanelGroupInterior;
